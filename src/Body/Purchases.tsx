// src/pages/home.tsx
import { useNavigate } from "react-router-dom";

export default function Sales() {
  const navigate = useNavigate();

  return (
    <div>
      <h1>purchases</h1>
    </div>
  );
}